<div class = "jumbotron">

  <h1>Bienvenue sur EventBriitee</h1>
		<h4>Tu trouveras les meilleures soirées de ta ville ou d'ailleurs...</h4>
		</br>
			<h6>Pour proposer un evenement, c'est <a> <%= link_to "ici !", new_event_path %> </a> </h6>
</div>


 <% if flash[:success] %>
      <div class="alert alert-success" role="alert"><%= flash[:success] %></div>
 <% elsif flash[:danger] %>
      <div class="alert alert-danger" role="alert"><%= flash[:danger] %></div>
   <% end %>

    <h5 class="h1-responsive font-weight-bold my-5">Voici les événements à venir...</h5>


<section>
<div class="card-deck">
<%Event.all.each do |event|%>
<div class="col-sm-5 mb-3">
<div class="card">
  <div class="card-header">
    <%=event.start_date%>
  </div>
  <div class="card-body align-center">
    <blockquote class="blockquote mb-0">
      <p><%=event.title%></p>
      <footer class="blockquote-footer">
      <div class="row justify-content-between align-self-center mx-3">
        <p><%=event.price%> €</p>
        <a class="btn btn-bg bg-primary" <%= link_to "show me!", event_path(event.id)%></a>
      </div>
      </footer>
    </blockquote>
  </div>
</div>
</div>
<%end%>
</div>
</section>
